# Copie para .env.local para desenvolvimento (opcional — o padrão já é localhost:8000).
# Em produção (GitHub Pages), esta variável é configurada em:
# Settings > Secrets and variables > Actions > aba "Variables" do repositório.
VITE_API_BASE_URL=http://localhost:8000
