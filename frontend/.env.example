# Copie para .env.local para desenvolvimento (opcional — o padrão já é localhost:8000).
# Em produção, esta variável é configurada no painel da Vercel, não neste arquivo.
VITE_API_BASE_URL=http://localhost:8000
