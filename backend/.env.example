# Copie este arquivo para .env e ajuste os valores conforme necessário.
CORS_ALLOWED_ORIGINS=["http://localhost:5173"]

# Obtenha uma chave gratuita em https://www.deepl.com/pro-api
DEEPL_API_KEY=4683080c-00b9-4f2e-af42-c3983a42afff:fx

# Connection string do Postgres do Supabase — use o "Session pooler"
# (Connect > Session pooler no painel do Supabase), NÃO a "Direct connection"
# (essa só existe em IPv6, que o Render não suporta).
# IMPORTANTE: troque "postgresql://" por "postgresql+asyncpg://" no início
DATABASE_URL=postgresql+asyncpg:///postgres.brfbzxsusfulppirypjx:[ATLAS_DB#2026!vK9@pL7$xQ42]@aws-0-ca-central-1.pooler.supabase.com:5432/postgres

# Credenciais da área do administrador — escolha um usuário e senha fortes.
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
# Chave secreta pra assinar os tokens de login. Gere uma com:
# python -c "import secrets; print(secrets.token_hex(32))"
AUTH_SECRET_KEY=20202020202020202020202020202020202020

# Supabase Storage (upload de vídeos) — Settings > API no painel do Supabase.
# ATENÇÃO: use a chave "service_role" (não a "anon"), e nunca exponha
# essa chave no frontend — ela só deve existir aqui, no backend.
SUPABASE_URL=https://brfbzxsusfulppirypjx.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImJyZmJ6eHN1c2Z1bHBwaXJ5cGp4Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4NDQxMjMwNiwiZXhwIjoyMDk5OTg4MzA2fQ.sthG0GP_5u4tIGcT5jm6NbYYCRN1Z3KxW3TY6blQzHw
SUPABASE_STORAGE_BUCKET=atlas-media

# Só e-mails terminados nisso podem se cadastrar.
ALLOWED_EMAIL_DOMAIN=@edu.sapiranga.rs.gov.br

